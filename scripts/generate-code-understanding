#!/usr/bin/env bash
# ==============================================================================
# UADOS — Code Understanding Engine Generator Hook
# ==============================================================================

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WORK_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

echo "[Auto-Sync] Initiating Code Understanding Engine crawl..."
python3 "${WORK_DIR}/tools/analysis/doc_generator.py"
echo "[Auto-Sync] CODE_UNDERSTANDING.md successfully updated!"
